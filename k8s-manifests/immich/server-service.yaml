apiVersion: v1
kind: Service
metadata:
  name: immich-server
  namespace: immich
  labels:
    app: immich
    component: server
spec:
  type: NodePort
  selector:
    app: immich
    component: server
  ports:
    - port: 2283
      targetPort: 2283
      nodePort: 30283
      protocol: TCP
